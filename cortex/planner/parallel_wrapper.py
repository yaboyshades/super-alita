from __future__ import annotations

import os
import warnings

warnings.warn(
    "parallel_wrapper is temporarily stubbed pending repair; see issue RUP-14.",
    RuntimeWarning,
)

class ParallelWrapper:  # minimal placeholder
    def __init__(self, *args, **kwargs):
        raise NotImplementedError(
            "ParallelWrapper temporarily disabled (RUP-14)."
        )
