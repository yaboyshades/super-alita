# Chatmodes

This folder contains Copilot chatmodes. New schema-based validation applies to files with suffix `.chatmode.(yml|yaml|md)` only, to preserve compatibility with legacy markdown modes in this repository.
