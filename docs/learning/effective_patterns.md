# Effective Patterns Library

- Capture successful orchestration plays with links to passing test runs.
- Highlight telemetry signatures that correlated with high reward scores.
- Include reusable prompt snippets vetted through `.github/workflows/prompt-regression.yml`.
