# Quality Gates Reference

- Track mandatory checks: `pre-commit`, `pytest -q tests/runtime`, and contract validation scripts.
- Note latency thresholds and retry caps that trigger the guardian escalation.
- Embed links to `.github/workflows/enhanced-quality-gates.yml` and `.github/workflows/capability-validate.yml`.
