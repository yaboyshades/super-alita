# Common Errors Catalog

- Document schema mismatches, telemetry gaps, and tooling regressions observed in CI.
- Link failing builds from `.github/workflows/ci.yml` and `.github/workflows/python-quality-and-tests.yml`.
- Provide mitigations and guardrail updates for each recurring issue.
