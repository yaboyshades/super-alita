[pytest]
addopts = -q -p no:pytest_asyncio
asyncio_mode = auto
markers =
    integration_redis: marks tests that require a running redis instance
    integration_puter: marks tests that exercise the Puter API integration
