# /plan (data access focus)
- Propose explicit glob patterns per data source.
- Mark which need `recursive=True`.
- Add lineage and error handling requirements in plan.# /plan
- Read `.specify/memory/constitution.md`.
- Generate `plan.md`, `research.md`, `data-model.md`, and `contracts/` as per templates.
- Prefer absolute paths in any report. Ensure constitutional checks are annotated in plan.
