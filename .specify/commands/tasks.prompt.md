# /tasks (data access focus)
- Generate tests-first tasks for discovery/ingest using `globkit`.
- Include tmp_path-based tests for patterns and loaders.# /tasks
- Run `.specify/scripts/check-task-prerequisites.sh --json`.
- Create `tasks.md` with tests-first ordering; mark parallelizable tasks `[P]`.
- Reference explicit file paths for each task. No vague steps.
