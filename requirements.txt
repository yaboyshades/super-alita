# Core dependencies
pydantic>=2.0
protobuf  # Let pip solve version conflicts
pyyaml
numpy>=1.20.0
networkx>=2.5
redis>=4.0.0
python-dotenv>=0.19.0
orjson>=3.8.0
# optional but recommended: faster Redis protocol parsing
hiredis>=2.0.0

# Vector storage and embeddings
faiss-cpu>=1.7.0
chromadb>=0.4.0
sentence-transformers>=2.0.0
openai>=1.0
openai-agents>=0.1

# ML and evolution
scikit-learn>=1.2.0
scipy>=1.9.0
torch>=2.0.0  # CPU build; GPU extras in requirements-gpu.txt

# Graph export
# python-graphml>=1.0  # optional, install manually if available

# Testing
pytest>=7.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0

# Development and linting
ruff>=0.4.0
black
mypy

# Web framework and API
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
aiohttp>=3.8.0
websockets>=11.0

# LLM integrations
google-generativeai>=0.5.0

# ACP server and search tooling
acp-sdk>=0.1.0
tenacity>=8.0.0

# gRPC for external consensus service
grpcio>=1.60.0
grpcio-tools>=1.60.0
