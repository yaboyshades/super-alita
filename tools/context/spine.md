# CMA Spine (≤300 tokens)

- Guardrails: Library-First; Tests-First; Simplicity; Spec–Code Integrity; Security by default (no secrets, param queries).
- Workflow: /specify → /plan → /tasks → review; tests-first; CI gates.
- Artifacts: spec.md, plan.md, tasks.md (+ PEL front matter).
- If `spec_hash` or `plan_hash` missing/mismatched → fetch & sync before coding.

